import React from 'react'

// need to first make an api call which will give us the reviews
// then we will simply make a react component of review card and then we will use
// Carousel slider tag

// this will make use a slider

const Carousel = () => {
  return (
    <div>
      
    </div>
  )
}

export default Carousel
